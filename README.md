```python
logger.debug("这是 DEBUG 级别日志，用于调试信息")
logger.info("这是 INFO 级别日志，记录正常运行情况")
logger.warning("这是 WARNING 级别日志，表示可能有问题")
logger.error("这是 ERROR 级别日志，表示发生错误")
logger.critical("这是 CRITICAL 级别日志，表示严重错误")
```